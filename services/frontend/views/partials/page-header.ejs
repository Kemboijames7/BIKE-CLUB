<div class="page-header">
    <div class="container" style="max-width: 900px;">
        <h3 class="mb-0 fw-bold"><%= pageIcon %> <%= pageTitle %></h3>
        <% if (typeof pageSubtitle !== 'undefined') { %>
        <p class="mb-0 mt-1 text-white-50 small"><%= pageSubtitle %></p>
        <% } %>
    </div>
</div>